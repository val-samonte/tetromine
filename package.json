{
  "name": "tetromine",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "test": "node --test tests/engine.test.js"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "wrangler": "^4.136.3"
  }
}
